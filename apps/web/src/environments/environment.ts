export const environment = {
  production: false,
  apiBase: '/api',
  cloudinary: {
    // Update these values with your Cloudinary credentials.
    // 1. Sign up at cloudinary.com
    // 2. Settings → Upload → Upload Presets → create unsigned preset
    // 3. Paste your cloud name and preset name below
    cloudName: 'dy4rncf4y', // e.g. 'dy4rncf4y'
    uploadPreset: 'seo_platform_tasks', // e.g. 'seo_platform_tasks'
  },
};
